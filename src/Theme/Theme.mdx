---
name: Theme
route: /theme
---








import { Playground } from "docz";
import { Button, setTheme, useTheme, Input } from "../index";

# Theme

## Basic usage
Set theme color before render App
Color must following formats: #nnn, #nnnnnn, rgb(), rgba(), hsl(), hsla().
```jsx
import { setTheme } from '@doopage/react-ui-kit'

setTheme({
  primaryColor: '#551A8B',
})

ReactDOM.render(<App/>, document.getElementById("root"));
```

To get theme
```jsx
import { getTheme } from '@doopage/react-ui-kit'
const theme = getTheme()
```

useTheme
```jsx
import { useTheme } from '@doopage/react-ui-kit'
const MyComponent = () => {
  const theme = useTheme()
    return (
      <div>{theme.primaryColor}</div>
    )
}
```

## Playground
<Playground>
  {setTheme({
    primaryColor: '#551A8B'
  })}
  <Button color='primary'>
    Purple button
  </Button>
  <Input variant="outlined" label='purple input' />
</Playground>

<Playground>
  {() =>
  {
    const theme = useTheme()
    return (
      <>
        <Button color='primary'>
          {theme.primaryColor}
        </Button>
      </>
    )
  }}
</Playground>
